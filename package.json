{
  "name": "krishimitra",
  "version": "1.0.0",
  "description": "AI-Powered Sustainable Agriculture Platform",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "both": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
